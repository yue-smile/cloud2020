server:
  port: 9527

spring:
  application:
    name: cloud-gateway
#  cloud:
#    gateway:
#      routes:
#        - id: payment_routh  #路由的ID，没有固定规则但要求唯一，建议配合服务名
#          uri: http://localhost:8001 #匹配提供服务的路由地址
#          predicates:
#            - Path=/payment/hystrix/timeout/** #断言，路由地址和这个相匹配后进行路由
#
#        - id: payment_routh2
#          uri: http://localhost:8001
#          predicates:
#            - Path=/payment/hystrix/ok/**

eureka:
  instance:
    hostname: cloud-gate-service
  client:
    service-url:
      register-with-eureka: true
      fetch-registry: true
      defaultZone: http://eureka7003.com:7003/eureka